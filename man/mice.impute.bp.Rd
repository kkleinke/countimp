% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mice.impute.bp.R
\name{mice.impute.bp}
\alias{mice.impute.bp}
\alias{mice.impute.bp.boot}
\alias{mice.impute.bnb}
\alias{mice.impute.bnb.boot}
\title{Bounded count imputation}
\usage{
mice.impute.bp(y, ry, x, wy = NULL, bounds = NULL, EV = FALSE, ...)

mice.impute.bp.boot(y, ry, x, wy = NULL, bounds = NULL, EV = FALSE, ...)

mice.impute.bnb(y, ry, x, wy = NULL, bounds = NULL, EV = FALSE, ...)

mice.impute.bnb.boot(y, ry, x, wy = NULL, bounds = NULL, EV = FALSE, ...)
}
\arguments{
\item{y}{Incomplete numeric count vector.}

\item{ry}{Logical vector: \code{TRUE} where \code{y} is observed.}

\item{x}{Numeric matrix or data frame of predictors, complete.}

\item{wy}{Logical vector: positions to impute. Defaults to \code{!ry}.}

\item{bounds}{Numeric vector \code{c(lower, upper)}. Whole numbers;
\code{Inf} is allowed as the upper bound only when \code{lower} is 0 or 1.
When passed through \code{\link{countimp}} this may also be a named list
with one entry per bounded variable, e.g.
\code{bounds = list(sym = c(0, 8), days = c(0, 5))}.}

\item{EV}{Logical. If \code{TRUE}, imputations flagged as extreme are
redrawn. Values that leave the bounds during that step are redrawn from the
truncated distribution, not clamped.}

\item{...}{Ignored, present for compatibility with the method contract.}
}
\value{
A numeric vector of length \code{sum(wy)} with the imputed counts,
  all of them within \code{bounds}.
}
\description{
Imputes a count variable with a structural lower and upper bound under an
interval-truncated Poisson (\code{bp}) or negative binomial (\code{bnb})
model. Both come in a Bayesian variant, which draws the regression
coefficients from their sampling distribution before drawing the counts, and
a bootstrap variant (suffix \code{.boot}).
}
\details{
Use these methods when the scale itself has a ceiling: symptoms endorsed out
of eight, days absent in a five-day week, items correct out of ten. Imputing
such a variable with \code{"poisson"} or \code{"nb"} produces values the
scale does not contain.

\strong{The bound enters the draw, not a post-hoc correction.} The obvious
repair -- draw unbounded, then clamp with \code{pmin(pmax(y, lo), hi)} -- is
wrong, and not only by a constant factor. Measured on a Poisson bounded to
\code{[1, 7]}, the ratio of clamped to correct variance is 0.74 at
\code{mu = 1}, 1.16 at \code{mu = 4} and 0.00 at \code{mu = 20}: clamping
inflates variance in the interior of the range and destroys it once the fitted
mean leaves the interval. There is no correction that repairs a clamped draw,
which is why these methods draw from the truncated distribution directly.

\strong{The bound also enters the likelihood.} When mass of the fitted
distribution lies beyond the ceiling, an unbounded fit reads the piled-up
ceiling observations as a flatter relationship. On 3000 cases with a true
slope of 0.45, the unbounded estimate falls to 0.31 at \code{hi = 4} while the
interval-truncated estimate stays at 0.46. Where the bound does not bind the
two agree, so the truncated fit costs nothing in that case.

Observed values outside \code{bounds} are an error, not a warning: they mean
the declared bounds are wrong, or the variable is not bounded at all.

For a lower bound of 1 with no ceiling, use \code{\link{mice.impute.ztp}}
instead -- it needs no grid and is the same model.
}
\examples{
set.seed(1)
n  <- 300
x1 <- rnorm(n)
y  <- rbinom(n, 8, plogis(0.3 + 0.5 * x1))   # a count capped at 8
y[sample(n, 60)] <- NA
d  <- data.frame(y = y, x1 = x1)
imp <- countimp(d, method = c(y = "bp", x1 = ""), m = 2,
                bounds = c(0, 8), printFlag = FALSE)
range(unlist(imp$imp$y))

}
\seealso{
\code{\link{mice.impute.ztp}} for a lower bound of 1 without a
  ceiling, \code{\link{countimp_fit_diag}} for choosing a count family.
}
